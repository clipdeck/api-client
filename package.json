{
  "name": "@clipdeck/api-client",
  "version": "0.1.0",
  "description": "Type-safe HTTP client for ClipDeck microservice APIs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@clipdeck/types": "file:../shared-types",
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.7.2",
    "vitest": "^4.0.18"
  }
}
